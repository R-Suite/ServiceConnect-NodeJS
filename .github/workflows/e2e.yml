name: e2e

on:
  pull_request:
    branches: [v3]
    types: [labeled, synchronize]
  push:
    branches: [v3]

jobs:
  e2e:
    if: github.event_name == 'push' || contains(github.event.pull_request.labels.*.name, 'e2e')
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "no e2e tests in Phase A; see Phase C spec"
