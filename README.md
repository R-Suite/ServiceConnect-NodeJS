# ServiceConnect (v3 in progress)

This branch hosts the v3 rewrite of `service-connect`. The current shipping version (v2.x) lives on `master`.

Phase A — Foundations — is in progress. The build does not produce a working messaging library yet.
